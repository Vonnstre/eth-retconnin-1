# Comma-separated RPC endpoints (public). E.g.
# https://cloudflare-eth.com,https://rpc.ankr.com/eth
ETH_RPC_URL=https://cloudflare-eth.com

# Price source (coingecko or manual)
ETH_PRICE_SOURCE=coingecko

# If manual, set ETH_PRICE_USD=...
ETH_PRICE_USD=4000

# Optional token map override (JSON string). Leave empty to use defaults built-in.
TOKENS_JSON=

# Harvest lookback (blocks)
HARVEST_LOOKBACK=18000

# Optional: path to operator private key for signing (use CI secret or local file). If empty,
# pipeline will generate an ephemeral key in-memory and publish only the public key & signatures.
SIGNING_KEY_PATH=
